// src/config/assets.js
// ── Cấu hình ảnh & media toàn app ───────────────────────────
// Chỉ cần sửa file này khi muốn đổi ảnh, không cần đụng vào component

const ASSETS = {
    /**
     * Ảnh nền trang chủ — Hero banner
     * Hiện tại: Ảnh đảo Cô Tô nhìn từ trên cao
     * Để đổi ảnh: thay URL bên dưới
     */
    HOME_HERO_BG: '/images/anh-co-to-1.jpg',
    /**
     * Logo hệ thống (nếu có)
     */
    LOGO: null,
};

export default ASSETS;